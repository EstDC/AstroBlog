specVersion: "v1beta"
serviceId: "astroblog"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "astroblog-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
